# 机器学习实验：基于 Word2Vec 的情感预测

## 1. 学生信息
- **姓名**：孙意
- **学号**：112304260123
- **班级**：数据1231

> 注意：姓名和学号必须填写，否则本次实验提交无效。

---

## 2. 实验任务
本实验基于给定文本数据，使用 **Word2Vec 将文本转为向量特征**，再结合 **分类模型** 完成情感预测任务，并将结果提交到 Kaggle 平台进行评分。

本实验重点包括：
- 文本预处理
- Word2Vec 词向量训练或加载
- 句子向量表示
- 分类模型训练
- Kaggle 结果提交与分析

---

## 3. 比赛与提交信息
- **比赛名称**：Bag of Words Meets Bags of Popcorn
- **比赛链接**：https://www.kaggle.com/competitions/word2vec-nlp-tutorial/overview
- **提交日期**：2026-04-15

- **GitHub 仓库地址**：https://github.com/mtdhk666/112304260123sunyi.git
- **GitHub README 地址**：https://github.com/mtdhk666/112304260123sunyi/blob/master/README.md

> 注意：GitHub 仓库首页或 README 页面中，必须能看到“姓名 + 学号”，否则无效。

---

## 4. Kaggle 成绩
请填写你最终提交到 Kaggle 的结果：

- **Public Score**：[待提交后填写]
- **Private Score**（如有）：[待提交后填写]
- **排名**（如能看到可填写）：[待提交后填写]

---

## 5. Kaggle 截图
请在下方插入 Kaggle 提交结果截图，要求能清楚看到分数信息。

![Kaggle截图](./images/kaggle_score.png)

> 建议将截图保存在 `images` 文件夹中。  
> 截图文件名示例：`112304260123_孙意_kaggle_score.png`

---

## 6. 实验方法说明

### （1）文本预处理
请说明你对文本做了哪些处理，例如：
- 分词
- 去停用词
- 去除标点或特殊符号
- 转小写

**我的做法：**  
1. **去除 HTML 标签**：使用 BeautifulSoup 去除评论中的 HTML 标签（如 &lt;br /&gt;）
2. **去除非字母字符**：使用正则表达式保留只包含字母的单词
3. **转小写**：将所有单词转换为小写，统一处理
4. **去停用词**：使用手动定义的英文停用词表去除无意义词汇（如 the, is, and 等）
5. **词干提取**：使用 PorterStemmer 进行词干提取，将单词还原为词干形式
6. **分词**：将处理后的文本分割为单词列表

---

### （2）Word2Vec 特征表示
请说明你如何使用 Word2Vec，例如：
- 是自己训练 Word2Vec，还是使用已有模型
- 词向量维度是多少
- 句子向量如何得到（平均、加权平均、池化等）

**我的做法：**  
1. **自己训练 Word2Vec**：使用训练集和无标签集的所有评论进行训练
2. **词向量维度**：设置为 300 维
3. **训练参数**：
   - window=5：上下文窗口大小为 5
   - min_count=10：只保留出现次数≥10 的词
   - sg=1：使用 Skip-gram 模型（而非 CBOW）
   - epochs=10：训练 10 轮
4. **句子向量表示**：对句子中所有词的词向量取**平均值**作为句子向量

---

### （3）分类模型
请说明你使用了什么分类模型，例如：
- Logistic Regression
- Random Forest
- SVM
- XGBoost

并说明最终采用了哪一个模型。

**我的做法：**  
1. **Bag of Words 模型**：使用 TF-IDF 向量化 + 逻辑回归分类器
   - TF-IDF 特征数：5000
   - ngram_range：(1, 2)，即考虑 uni-gram 和 bi-gram
   - 分类器：Logistic Regression (C=1.0, max_iter=500)

2. **Word2Vec 模型**：使用 Word2Vec 词向量 + 逻辑回归分类器
   - 词向量维度：300
   - 分类器：Logistic Regression (C=1.0, max_iter=500)

3. **最终采用**：Bag of Words 模型（交叉验证 AUC 更高）

---

## 7. 实验流程
请简要说明你的实验流程。

示例：
1. 读取训练集和测试集  
2. 对文本进行预处理  
3. 训练或加载 Word2Vec 模型  
4. 将每条文本表示为句向量  
5. 用训练集训练分类器  
6. 在测试集上预测结果  
7. 生成 submission 文件并提交 Kaggle  

**我的实验流程：**  
1. **加载数据**：读取 labeledTrainData.tsv、testData.tsv 和 unlabeledTrainData.tsv
2. **文本预处理**：对所有数据集的评论进行清洗、去停用词、词干提取和分词
3. **Bag of Words 模型训练**：
   - 使用 TF-IDF 将文本转换为向量
   - 训练逻辑回归分类器
   - 5折交叉验证评估模型性能（AUC=0.9523）
4. **Word2Vec 模型训练**：
   - 使用所有数据（训练集+无标签集）训练 Word2Vec 词向量
   - 将每条评论转换为平均词向量
   - 训练逻辑回归分类器（训练集 AUC=0.9505）
5. **预测与提交**：
   - 分别使用两个模型对测试集进行预测
   - 生成 submission_bow.csv 和 submission_word2vec.csv
   - 将结果提交到 Kaggle 平台

---

## 8. 文件说明
请说明仓库中各文件或文件夹的作用。

**我的项目结构：**
```text
112304260123sunyi/
├─ code/                 # 代码文件
│  ├─ preprocessing.py  # 文本预处理模块
│  ├─ bow_model.py      # Bag of Words 模型
│  ├─ word2vec_model.py # Word2Vec 模型
│  ├─ main.py           # 主程序
│  └─ requirements.txt  # 依赖包
├─ report/               # 实验报告
│  ├─ 实验报告.md        # 完整实验报告
│  └─ readme_机器学习实验2模板.md # 实验模板
├─ results/              # 实验结果
│  ├─ submission_bow.csv      # Bag of Words 模型预测结果
│  └─ submission_word2vec.csv # Word2Vec 模型预测结果
├─ labeledTrainData.tsv/ # 训练集数据
├─ testData.tsv/         # 测试集数据
├─ unlabeledTrainData.tsv/ # 无标签数据
├─ .gitignore            # Git 忽略文件
└─ README.md             # 仓库说明
```

---

## 9. 实验结果与分析（可选）

### 模型性能对比
| 模型 | 交叉验证 AUC | 训练集 AUC |
|------|-------------|------------|
| Bag of Words (TF-IDF + LR) | 0.9523 | - |
| Word2Vec (300d + LR) | - | 0.9505 |

### 实验心得
- 文本预处理对模型性能有重要影响，特别是去停用词和词干提取
- Bag of Words 模型在本任务中表现略优于 Word2Vec 模型
- 增加 n-gram 特征可以捕获更多的上下文信息
- 合理使用无标签数据可以提升 Word2Vec 词向量的质量
